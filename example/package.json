{
  "private": true,
  "name": "@u-wave/react-translate-example",
  "description": "React-Translate example.",
  "version": "0.0.0-example",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle app.js --loader:.js=jsx > bundle.js",
    "start": "ecstatic ."
  },
  "dependencies": {
    "@u-wave/react-translate": "file:..",
    "@u-wave/translate": "^1.0.0",
    "ecstatic": "^4.0.0",
    "esbuild": "^0.14.29",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
