{
  "private": true,
  "name": "react-translate-example",
  "description": "React-Translate example.",
  "version": "0.0.0-example",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "esbuild --bundle app.js --loader:.js=jsx > bundle.js",
    "start": "ecstatic ."
  },
  "dependencies": {
    "@u-wave/react-translate": "file:..",
    "@u-wave/translate": "^1.0.0",
    "ecstatic": "^4.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.25"
  }
}
